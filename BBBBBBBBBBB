# do not attempt to read or set password here
if pname is not None:
    key = str(pname)
    # skip any password-like property
    if key.lower().find('password') == -1:
        if pval is None:
            props[key] = None
        else:
            try:
                props[key] = str(pval)
            except:
                props[key] = "%s" % pval