if pname in ('DataSourceName', 'xADataSourceName'):
    if pval is None:
        entry[pname] = None
    else:
        try:
            entry[pname] = str(pval)
        except:
            entry[pname] = "%s" % pval