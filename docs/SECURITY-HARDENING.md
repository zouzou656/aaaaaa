# Security Hardening

- Integrate with Keycloak using OIDC for RBAC and JWT propagation.
- Enforce TLS at the reverse proxy or application server level.
- Secure secrets via external vault references such as `vault:` URIs.
