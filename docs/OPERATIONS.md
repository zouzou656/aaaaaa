# Operations Guide

- Monitor MicroProfile health at `/api/healthz`.
- Use provided scripts under `deploy/systemd` and `deploy/windows` to run as services.
- Message replay is controlled by the orchestration engine with audit events.
